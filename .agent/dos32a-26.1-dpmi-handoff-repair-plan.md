# Repair DOS/32A 26.1 DPMI handoff behavior

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

This plan follows `.agent/PLANS.md` from the repository root.

## Purpose / Big Picture

DOS/32A 26.1 must be a trustworthy DOS/4GW replacement, not a set of game-name shortcuts. A user with HDPMI32 loaded should be able to run both bound games, where DOS/32A is embedded in the game executable, and external-launch games, where a game starts a separate `DOS4GW.EXE` replacement. BUILD-engine games need the external HDPMI32 path for linear frame buffer write combining, and id Tech games should not freeze merely because HDPMI32 is loaded.

The visible outcome is a rebuilt `binw\dos32a.exe` whose protected-mode host selection is controlled by the documented DPMI/VCPI detection order and whose external-DPMI setup behaves close to historical DOS/32A 7.x rather than masking problems by recognizing game names.

## Progress

- [x] (2026-05-25) Read `.agent/PLANS.md` and confirmed this task requires a self-contained living plan.
- [x] (2026-05-25) Confirmed the branch is `26.1` and clean against `origin/26.1`.
- [x] (2026-05-25) Re-read the earlier id Tech HDPMI plan and identified that its conclusion, avoiding HDPMI32 for id Tech, is not acceptable for this release goal.
- [x] (2026-05-25) Located the current DOS4GW-name HDPMI preference in `src/dos32a/text/client/misc.asm`, `src/dos32a/text/kernel/detect.asm`, `src/dos32a/kernel.asm`, and `src/dos32a/dos32a.asm`.
- [x] (2026-05-25) Confirmed that Duke3D is a bound launch mode: there is no external `DOS4GW.EXE`, so any fix tied to the extender executable name does not cover Duke3D.
- [x] (2026-05-25) Replaced the name-based HDPMI shortcut with normal DPMI/VCPI order semantics.
- [x] (2026-05-25) Reduced external-DPMI exception-vector takeover to the historical four vectors used by DOS/32A 7.10/7.35/8.00.
- [x] (2026-05-25) Built `binw/dos32a.exe` with TASM 5.3 and Open Watcom; SHA-256 is `3DA6E32ED97786BB9A1A61E407AC5B12CB1F39C58E619696B77C53BFC1050ABA`.
- [x] (2026-05-25) Ran `cmd /c smoke-dosbox.cmd -TimeoutSeconds 10`; SVER, direct-loader, DOS32A, and app smoke paths passed.
- [x] (2026-05-25) Ran a drive Z-only DOSBox-X smoke with HDPMI32 copied into the scratch folder; `HELLO.EXE` ran and printed `HDPMI_SMOKE_OK` under `/DPMITST:ON`.
- [x] (2026-05-25) Updated `ChangeLog` to describe the DPMI handoff repair instead of the removed DOS4GW-name workaround.
- [x] (2026-05-25) Packaged a real-hardware candidate at `dist/dpmi-handoff-repair-26.1.zip`; SHA-256 is `CCECC5ABB9726C9763E7CAA1EEA699A504191E4BD7F637635D6DEE3C41F4CA02`.
- [x] (2026-05-25) Staged the same candidate to `C:\DOSBox-X\drivec\DOS32A\BINW\DOS32A.EXE`, backing up the previous file as `DOS32A.PRE`.
- [x] (2026-05-25) Source, plan, and ChangeLog are ready to commit and push after local validation; generated `dist` artifacts remain ignored.

## Surprises & Discoveries

- Observation: The documentation conflicts with itself about `/DPMITST`.
  Evidence: `dos32a_800/docs/html/user/4.html` says `/DPMITST:ON` looks for a DPMI host first, but `src/dos32a/kernel.asm` and `dos32a_800/docs/html/tech/10.html` define bit 0 as `0=test DPMI/VCPI, 1=VCPI/DPMI /1=default`. The setup UI in `src/ss/menus.c` also displays bit set as `VCPI/DPMI`.

- Observation: Current 26.1 contains a branch-specific HDPMI preference that is not inherited from DOS/32A 7.10, 7.35, or 8.00.
  Evidence: `src/dos32a/text/kernel/detect.asm` has `@@detect_HDPMI`, `src/dos32a/kernel.asm` exports `pm32_hdpmi`, and `src/dos32a/text/client/misc.asm` sets that byte when launched as `DOS4GW.EXE`.

- Observation: The current name-based policy cannot help bound launch modes.
  Evidence: Duke3D in `C:\DOSBox-X\drivec\DUKE3D` has `DUKE3D.EXE` and no external `DOS4GW.EXE`; the game starts DOS/32A from the embedded stub, so checking whether the extender process name is `DOS4GW.EXE` never fires.

- Observation: DOS/32A 7.10/7.35 installed only four protected-mode exception handlers under an external DPMI host.
  Evidence: `dos32a-735-src/src/dos32a/text/client/misc.asm` and `dos32a_800/src/dos32a/text/client/misc.asm` install handlers for exceptions 00h, 06h, 0Dh, and 0Eh after checking `_sys_type == 3`. Current 26.1 installs handlers 00h through 0Eh.

- Observation: The tracked assembly sources preserve mixed historical line endings.
  Evidence: `git ls-files --eol` reports `i/mixed w/mixed` for most touched assembly files. A mechanical CRLF conversion made `git diff --check` report carriage returns as trailing whitespace, so the source must be left in its existing mixed style.

## Decision Log

- Decision: Treat the DOS4GW-name HDPMI preference as a temporary workaround to remove from the next candidate.
  Rationale: It recovers BUILD write combining for some external-DOS4GW games, but it does not explain or repair why normal DPMI detection and handoff fail under HDPMI32. It also cannot cover bound games such as Duke3D.
  Date/Author: 2026-05-25 / Codex

- Decision: Preserve DOS/4GW compatibility defaults separately from HDPMI forcing.
  Rationale: The archived `DOS4GW.D32` defaults are a launch-compatibility preset. Forcing a specific DPMI host is a different policy and should not be hidden inside those defaults.
  Date/Author: 2026-05-25 / Codex

- Decision: First repair detection order, then test a narrow rollback of external-DPMI exception hook scope.
  Rationale: Detection order determines whether HDPMI32 is selected at all. Exception hook scope is a plausible freeze cause after handoff because current 26.1 intercepts debug and breakpoint exceptions under external DPMI while 7.x did not.
  Date/Author: 2026-05-25 / Codex

- Decision: Make `/DPMITST:ON` select DPMI-first detection in the environment parser.
  Rationale: The user manual documents `/DPMITST:ON` as "look for a DPMI host first", and the user's hardware batch already uses that switch. The internal bit remains `0=DPMI/VCPI, 1=VCPI/DPMI`, so the parser now clears bit 0 for ON and sets it for OFF.
  Date/Author: 2026-05-25 / Codex

## Outcomes & Retrospective

The first repair candidate builds and passes the local DOSBox-X smoke test plus a drive Z-only HDPMI32 smoke. The candidate removes the hidden `pm32_hdpmi` branch, makes `/DPMITST:ON` select DPMI-first detection, removes the XMS/IOPL shortcut that skipped the configured DPMI/VCPI probes, and limits external-DPMI exception hooks to the historical four-vector set. Real-hardware testing is still required because DOSBox-X does not reproduce the game freeze reliably.

## Context and Orientation

The repository root is `C:\Users\mbeutler\Projects\dos32a`. The maintained extender source is under `src/dos32a`. Historical source trees are available under `dos32a-735-src/src/dos32a` and `dos32a_800/src/dos32a`.

DPMI means DOS Protected Mode Interface. It is the service API a protected-mode DOS program uses for memory, selectors, interrupts, and real-mode calls. HDPMI32 is an external DPMI host; when DOS/32A chooses it, HDPMI32 performs part of the protected-mode service work. VCPI means Virtual Control Program Interface. It is an older protected-mode interface often provided by memory managers. Linear frame buffer write combining means the CPU or host combines video-memory writes for better throughput; on this hardware it gives a large BUILD-engine frame-rate improvement when the game maps the VESA linear frame buffer through HDPMI32.

There are two relevant launch modes. In a bound launch, the DOS/32A stub is embedded in the game executable, such as `DUKE3D.EXE` or id Tech executables. In an external launch, the game starts a separate extender executable named `DOS4GW.EXE`, such as Redneck Rampage and S2/SR. A correct fix must be compatible with both modes because DOS/4GW supports both.

The main detection code is `src/dos32a/text/kernel/detect.asm`. It sets `pmodetype` to external DPMI, VCPI, XMS, or raw mode before the kernel initializes. The main client setup code is `src/dos32a/text/client/misc.asm`. It installs protected-mode interrupt and exception handlers after the extender has entered protected mode.

## Plan of Work

First, remove the HDPMI32-specific preference byte and helper from normal source. Keep `apply_dos4gw_name_config` only for DOS/4GW compatibility defaults, and remove the call that sets a hidden HDPMI preference. This makes bound and external launch modes use the same detection rules.

Second, make `/DPMITST` behavior explicit and consistent with the code. The kernel bit currently means `0=DPMI first` and `1=VCPI first`. Because the public user documentation says `/DPMITST:ON` means DPMI first, the repair should either change `cfg_env_test` to match the user documentation or document that the setup UI and technical docs define the true behavior. The candidate will favor user-visible command-line behavior: `/DPMITST:ON` should clear the bit so DPMI is tried before VCPI, and `/DPMITST:OFF` should set the bit so VCPI is tried before DPMI.

Third, remove the XMS/IOPL shortcut that skips DPMI and VCPI tests before honoring the configured order. The historical 7.x and 8.00 detection code checks XMS availability, then checks DPMI/VCPI in the configured order, then falls back to raw or XMS operation. That is the behavior needed for HDPMI32 to be selected without a game-name workaround.

Fourth, under an external DPMI host, install only exception handlers 00h, 06h, 0Dh, and 0Eh. Current 26.1 installs 00h through 0Eh, including debug and breakpoint traps, which can intercept game or DPMI-host behavior that historical DOS/32A did not touch. For non-external-DPMI paths, leave the current handler set unchanged in this first candidate to avoid broadening the change.

Fifth, build with `cmd /c build.cmd dos32a`, package the rebuilt `binw\dos32a.exe`, and run the local smoke script with a 10-second timeout. DOSBox-X cannot prove the real-hardware HDPMI32 path, but it can catch build, launch, and immediate-crash regressions.

Implemented candidate source changes:

    src/dos32a/dos32a.asm:
        Removed the external declaration for pm32_hdpmi.

    src/dos32a/kernel.asm:
        Removed the exported pm32_hdpmi byte.

    src/dos32a/text/client/config.asm:
        Changed cfg_env_test so /DPMITST:ON clears bit 0 and /DPMITST:OFF sets bit 0.

    src/dos32a/text/client/misc.asm:
        Kept DOS4GW.EXE compatibility defaults but removed the HDPMI force call. Under _sys_type == 3, install and restore only exceptions 00h, 06h, 0Dh, and 0Eh.

    src/dos32a/text/kernel/detect.asm:
        Removed @@detect_HDPMI and removed the XMS/IOPL shortcut that skipped DPMI/VCPI tests.

    ChangeLog:
        Replaced the obsolete DOS4GW-name workaround entry with DPMITST, DPMI/VCPI probing, and external-DPMI exception scope notes.

## Concrete Steps

From `C:\Users\mbeutler\Projects\dos32a`, inspect and edit:

    src/dos32a/dos32a.asm
    src/dos32a/kernel.asm
    src/dos32a/text/client/config.asm
    src/dos32a/text/client/misc.asm
    src/dos32a/text/kernel/detect.asm

Build:

    cmd /c build.cmd dos32a

Smoke test:

    cmd /c smoke-dosbox.cmd -TimeoutSeconds 10

Observed smoke result:

    SVER -- Version Reporting Utility version 26.1
    Version:        26.1
    SVER_SMOKE_OK
    DIRECT_LOADER_SMOKE_OK
    DOS32A_SMOKE_OK
    APP_SMOKE_OK
    DOSBox-X smoke passed using temporary staged files in C:\DOSBox-X\drivez\D32SE75B and C:\DOSBox-X\drivez\D32AE75B.

Observed HDPMI smoke result:

    HDPMI32 now resident
    Hello world from protected mode!!!
    HDPMI_SMOKE_OK

The first HDPMI smoke attempt used `/VERBOSE:ON` and timed out. The log showed `System: DPMI` before the timeout, and the timeout was expected because verbose mode waits for a key before transferring to the app. The second attempt omitted verbose mode and completed under the 10-second limit.

Package the candidate under `dist` with a manifest that records the source commit or worktree status, the SHA-256 hash of `binw\dos32a.exe`, and the exact real-hardware test matrix.

## Validation and Acceptance

Local acceptance requires a clean build, a passing 10-second DOSBox-X smoke run, no leftover DOSBox-X processes, and a package containing the rebuilt extender plus a manifest.

Real-hardware acceptance requires these observations with HDPMI32 loaded:

1. Bound id Tech executables do not freeze when DOS/32A selects HDPMI32 through normal `/DPMITST:ON` detection.
2. Bound Duke3D is not dependent on an external `DOS4GW.EXE` name rule.
3. Redneck Rampage keeps the linear frame buffer write-combining performance improvement.
4. S2/SR 65k color mode remains working after the selector-pair fix.

If a candidate fails, the result must still be useful: the manifest should make clear whether detection order, exception-vector scope, or a later DPMI service is the likely remaining fault.

## Idempotence and Recovery

All generated packages should live under `dist`. Do not overwrite `.BAK` game executables under `C:\DOSBox-X\drivec`. If DOSBox-X hangs during a local test, terminate only DOSBox-X processes and keep the iteration under 10 seconds.

If the source candidate regresses local smoke, use `git diff` to isolate only the files edited by this plan, then adjust those edits rather than reverting unrelated user changes.

## Artifacts and Notes

Important known source facts:

    src/dos32a/kernel.asm:
        pm32_mode bit 0 currently means 0=DPMI/VCPI, 1=VCPI/DPMI.

    src/dos32a/text/client/config.asm:
        cfg_env_test currently sets bit 0 for /DPMITST:ON, which makes ON mean VCPI first in current code.

    dos32a_800/docs/html/user/4.html:
        The user manual says /DPMITST:ON looks for a DPMI host first.

    src/dos32a/text/client/misc.asm:
        Current 26.1 installs exception handlers 00h through 0Eh.

    dos32a-735-src/src/dos32a/text/client/misc.asm:
        Historical external-DPMI code installs only 00h, 06h, 0Dh, and 0Eh.

## Interfaces and Dependencies

The build depends on Open Watcom at `C:\WATCOM`, TASM 5.3 in `tasm5`, and DOSBox-X at `C:\DOSBox-X\dosbox-x.exe`. The runtime real-hardware tests depend on HDPMI32 loaded from the target DOS environment, with the user's current `C:\DOSBox-X\drivec\TWEAKS.BAT` as the closest local reference for environment variables.

Revision note, 2026-05-25 / Codex: Initial plan created after rejecting the DOS4GW-name HDPMI workaround and reframing the task as a DPMI detection and handoff repair.
