# Restore BUILD LFB Write Combining Without Regressing id Tech

This ExecPlan is a living document. The sections `Progress`, `Surprises & Discoveries`, `Decision Log`, and `Outcomes & Retrospective` must be kept up to date as work proceeds.

This plan follows `.agent/PLANS.md` from the repository root.

## Purpose / Big Picture

The user reports that linear frame buffer write combining is broken again in BUILD-engine games after the 26.1 S2/SR work. Write combining is the CPU or DPMI-host behavior that lets linear frame buffer writes be combined for much faster video output; on this hardware it previously produced about a 20 FPS Redneck Rampage improvement. The goal is to restore that BUILD behavior without reverting the id Tech HDPMI32 fix that keeps Doom-family games from freezing when HDPMI32 is loaded.

The observable result is a rebuilt `binw\dos32a.exe` that still contains the S2 selector-pair fix, still avoids forced HDPMI32 for Doom-family bound executables, and again forces the HDPMI32 external-DPMI path for known BUILD launch targets such as `RR.EXE`, `RA.EXE`, `RACING.EXE`, `DUKE3D.EXE`, `SW.EXE`, and `BLOOD.EXE`.

## Progress

- [x] (2026-05-24) Confirmed the working tree was clean on branch `26.1` at `f6346ae`.
- [x] (2026-05-24) Confirmed the current source contains the id Tech fix from `b77e519`, which removed the unconditional HDPMI32-specific early detection path.
- [x] (2026-05-24) Confirmed the current `C:\DOSBox-X\drivec\DOS32A\BINW\DOS32A.EXE` is the selector-pair candidate hash `27C9D5DBEDBF6BEADAD89AD2CD4CA0C2FCE0614A11E75E33FC709E10443D2487`.
- [x] (2026-05-24) Confirmed the older broad drive C package was built from commit `c54df59`, before the id Tech HDPMI detection fix, and therefore could still show BUILD WC even after the source changed.
- [x] (2026-05-24) Implemented a controlled HDPMI32 early-detection switch for BUILD launch targets. `apply_hdpmi_name_config` matches the current executable basename and first command-line token against known BUILD names, sets `pm32_hdpmi`, and `detect.asm` only calls the HDPMI32 shortcut when that flag is set.
- [x] (2026-05-24) Rebuilt with `cmd /c build.cmd dos32a`; the first attempt caught an 8086 illegal `[dx]` indexing mode in the name table matcher, then the corrected `[bx]` table pointer build passed.
- [x] (2026-05-24) Ran `cmd /c smoke-dosbox.cmd -TimeoutSeconds 10`; SVER, direct loader, standalone guard, and protected-mode sample all passed.
- [x] (2026-05-24) Staged the candidate to `C:\DOSBox-X\drivec\DOS32A\BINW\DOS32A.EXE`, `C:\DOSBox-X\drivec\S2\DOS4GW.EXE`, and `C:\DOSBox-X\drivec\SR\DOS4GW.EXE`. Candidate SHA-256 from the final local rebuild is `7ED9923FDDD31BB311E81DBF5109C8E3D902ABAB8E5777FCF46254F901AA3592`.
- [x] (2026-05-24) Ran a hidden DOSBox-X S2 `START65H.EXE` probe capped at 10 seconds. It timed out in the known emulated path and the log contained no DOS/32A exception, illegal descriptor, or triple-fault pattern.
- [x] (2026-05-24) Packaged `dist\build-wc-26.1-candidate.zip`; zip SHA-256 is `D98BB60218843DEB63D71C67556F850FB0C6A683273C5955F4E6ADFA7EEA52F1`.
- [x] (2026-05-24) Added a `ChangeLog` entry for the BUILD-specific HDPMI32 split and rechecked the diff with whitespace validation.
- [ ] Commit and push once the candidate source is coherent and locally validated.

## Surprises & Discoveries

- Observation: The regression is probably not caused directly by the S2 selector-pair allocation.
  Evidence: The only source change since the known `b77e519` id Tech fix in the HDPMI detection area is absent; the selector-pair commit only touched `src/dos32a/loader.asm`. The WC behavior instead depends on the earlier `detect.asm` decision to stop forcing HDPMI32 before the IOPL/XMS shortcut.

- Observation: The user's local broad game patch package predates the id Tech HDPMI fix.
  Evidence: `dist\dos32a-26.1-drivec-patch\MANIFEST.TXT` records source commit `c54df59`, while the id Tech fix is commit `b77e519`. That explains how BUILD WC could still appear to work while the source tree had already moved away from forced HDPMI32.

- Observation: The name-table matcher has to stay within 8086 addressing modes because it lives before the `.386p` section of `misc.asm`.
  Evidence: The first build failed with `TEXT\CLIENT\misc.asm(226) Illegal indexing mode` for `mov bl,[dx]`. Rewriting the table pointer to use `BX` fixed the build.

## Decision Log

- Decision: Do not revert the id Tech fix.
  Rationale: Restoring unconditional HDPMI32 detection would likely bring back the Doom-family freezes that `b77e519` fixed. The replacement must be narrower than the original 26.0 HDPMI shortcut.
  Date/Author: 2026-05-24 / Codex

- Decision: Force early HDPMI32 only for known BUILD-engine executable names.
  Rationale: The BUILD performance benefit depends on HDPMI32 being selected as the DPMI host before DOS/32A takes the XMS/IOPL shortcut. id Tech needs the opposite behavior. At this point the practical low-risk split is by launch target: known BUILD targets get the HDPMI32 path, while Doom-family executables continue using the non-forced detection path.
  Date/Author: 2026-05-24 / Codex

## Outcomes & Retrospective

Current outcome: a local candidate is built, smoke-tested, staged under `C:\DOSBox-X\drivec`, and packaged for real-hardware testing. The source fix is ready to commit and push.

## Context and Orientation

`src\dos32a\text\kernel\detect.asm` decides whether DOS/32A runs under an external DPMI host such as HDPMI32, under VCPI, or using its XMS/raw path. A DPMI host is software that provides protected-mode services to DOS programs. HDPMI32 can apply write combining to physical linear frame buffer mappings, which is why BUILD games run faster when DOS/32A chooses HDPMI32.

Commit `b77e519` removed an HDPMI32-specific early-detection shortcut because forcing HDPMI32 caused id Tech games to freeze on the user's real hardware. The current regression report indicates BUILD games need a way back to that early HDPMI32 path.

`src\dos32a\text\client\misc.asm` runs before `pm32_info` and already contains executable-name policy in `apply_dos4gw_name_config`. That makes it the right place to classify launch names before protected-mode detection begins. `src\dos32a\kernel.asm` owns the kernel-side variables read by `detect.asm`. `src\dos32a\text\client\data.asm` owns client-side strings and lookup tables.

## Plan of Work

Add a single kernel byte, `pm32_hdpmi`, that the real-mode client can set before calling `pm32_info`. Bit 0 means "try HDPMI32 before the normal detection order and before the IOPL/XMS shortcut." Keep the default at zero so Doom-family games remain on the fixed path.

In `src\dos32a\text\client\data.asm`, add a short length-prefixed table of known BUILD-engine executable basenames. Include both bare names and `.EXE` names for `RR`, `RA`, `RACING`, `DUKE3D`, `SW`, `SW_TD`, `BLOOD`, and `BUILD`.

In `src\dos32a\text\client\misc.asm`, add `apply_hdpmi_name_config`. It should inspect the current executable basename from the PSP environment tail and the first command-line token. If either basename matches the BUILD table, set `pm32_hdpmi` bit 0 in the kernel segment. Call this after `apply_dos4gw_name_config` and before `get_environ_config` in `src\dos32a\dos32a.asm`.

In `src\dos32a\text\kernel\detect.asm`, restore the small `@@detect_HDPMI` helper from the earlier 26.0 path, but call it only when `pm32_hdpmi` bit 0 is set. The calls should happen before the XMS IOPL shortcut and before the normal VCPI/DPMI order in the no-XMS branch.

Update `ChangeLog` to record that 26.1 restores BUILD write-combining by making HDPMI32 early detection launch-target specific.

## Concrete Steps

From `C:\Users\mbeutler\Projects\dos32a`, edit only the files named above and this plan. Then run:

    cmd /c build.cmd dos32a
    cmd /c smoke-dosbox.cmd -TimeoutSeconds 10

After a successful build, copy the candidate to:

    C:\DOSBox-X\drivec\DOS32A\BINW\DOS32A.EXE

If packaging is needed for real hardware, create a small package under `dist\build-wc-26.1-candidate` containing `BINW\DOS32A.EXE`, a DOS batch that copies it to `C:\DOS32A\BINW\DOS32A.EXE`, and a manifest with SHA-256 hashes.

## Validation and Acceptance

Local validation can only prove that the extender still builds and the generic DOSBox-X smoke path does not crash. Real acceptance requires the user's hardware:

1. With HDPMI32 loaded through the existing `TWEAKS.BAT`, a BUILD game such as Redneck Rampage should regain the write-combining performance improvement.
2. Doom-family games should not return to the HDPMI32 launch freeze fixed by `b77e519`.
3. S2 `START65H.EXE` should still pass the user's real-hardware 65k-color test because the selector-pair change remains in place.

## Idempotence and Recovery

The source edits are additive and can be reverted by one commit if the real-hardware split is wrong. The package copy step is safe to repeat because it overwrites only `C:\DOSBox-X\drivec\DOS32A\BINW\DOS32A.EXE`. Do not overwrite game executables during this plan unless a package script explicitly targets the user's real hardware and preserves backups.

## Artifacts and Notes

The key historical hashes are:

    Current selector-pair candidate DOS32A.EXE:
    27C9D5DBEDBF6BEADAD89AD2CD4CA0C2FCE0614A11E75E33FC709E10443D2487

    Older broad drive C package source commit:
    c54df59

    Current BUILD WC candidate DOS32A.EXE:
    7ED9923FDDD31BB311E81DBF5109C8E3D902ABAB8E5777FCF46254F901AA3592

    Current BUILD WC candidate zip:
    D98BB60218843DEB63D71C67556F850FB0C6A683273C5955F4E6ADFA7EEA52F1

## Interfaces and Dependencies

The new kernel variable is:

    pm32_hdpmi db 0

`src\dos32a\dos32a.asm` must declare it as an external byte so client code can write it after loading `DS` with `_seg_kernel`. `src\dos32a\text\kernel\detect.asm` must read it directly in the kernel segment.

Revision note, 2026-05-24 / Codex: Created this plan after the user reported that BUILD linear frame buffer write combining regressed again after the S2/SR selector-pair candidate.

Revision note, 2026-05-24 / Codex: Updated after implementing, building, smoke-testing, staging, and packaging the BUILD-target-specific HDPMI32 candidate.
